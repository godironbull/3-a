// 3.Login do Utilizador: Cria uma variável utilizador. Se for igual a "admin", imprime "Bem-vindo, administrador!", senão imprime "Bem-vindo, utilizador comum!".
let utilizador;

utilizador = prompt("adimin ? (s/n): ");

if(utilizador == "s"){
    console.log("Bem-vindo, administrador!");
}else{
    console.log("Bem-vindo, utilizador comum!");
}
