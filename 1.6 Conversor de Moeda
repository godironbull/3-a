#include <stdio.h>

int main()

{
    float reais,dolar;
    
    printf("Digite o valor em reais para conversao: ");
    scanf("%f", &reais);
    
    dolar = reais / 4.99;
    
    printf("o valor convertido e:$%.2f dolares", dolar);
    
    return 0;
}
